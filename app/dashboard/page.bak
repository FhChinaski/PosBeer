"use client";
import { useEffect } from "react";
import { useRouter } from "next/navigation";
import {
  LineChart,
  Line,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
  BarChart,
  Bar,
} from "recharts";

export default function DashboardPage() {
  const router = useRouter();

  useEffect(() => {
    const user = true;
    if (!user) router.push("/login");
  }, [router]);

  const ventasData = [
    { name: "Lun", ventas: 4200 },
    { name: "Mar", ventas: 6500 },
    { name: "Mié", ventas: 7200 },
    { name: "Jue", ventas: 8900 },
    { name: "Vie", ventas: 11000 },
    { name: "Sáb", ventas: 13600 },
    { name: "Dom", ventas: 7400 },
  ];

  const cervezasData = [
    { name: "IPA", pints: 120 },
    { name: "Stout", pints: 85 },
    { name: "Pale Ale", pints: 98 },
    { name: "Lager", pints: 150 },
  ];

  const stats = [
    { title: "Ventas Hoy", value: "$8,430", color: "bg-amber-500" },
    { title: "Cervezas Vendidas", value: "237", color: "bg-yellow-600" },
    { title: "Ticket Promedio", value: "$178", color: "bg-amber-700" },
    { title: "Propinas", value: "$920", color: "bg-yellow-800" },
  ];

  return (
    <div className="min-h-screen bg-amber-50 p-6">
      <header className="flex justify-between items-center mb-8">
        <h1 className="text-3xl font-bold text-amber-800">🍺 POSBeer Dashboard</h1>
        <button
          onClick={() => router.push("/login")}
          className="bg-amber-700 text-white px-4 py-2 rounded-lg hover:bg-amber-800"
        >
          Cerrar sesión
        </button>
      </header>

      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
        {stats.map((s) => (
          <div
            key={s.title}
            className={${s.color} p-6 text-white rounded-xl shadow-md}
          >
            <h2 className="text-lg font-semibold">{s.title}</h2>
            <p className="text-2xl font-bold">{s.value}</p>
          </div>
        ))}
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <section className="bg-white p-6 rounded-xl shadow-md">
          <h2 className="text-xl font-semibold mb-4 text-amber-800">
            Ventas Semanales
          </h2>
          <ResponsiveContainer width="100%" height={300}>
            <LineChart data={ventasData}>
              <CartesianGrid strokeDasharray="3 3" />
              <XAxis dataKey="name" />
              <YAxis />
              <Tooltip />
              <Line
                type="monotone"
                dataKey="ventas"
                stroke="#92400E"
                strokeWidth={3}
                activeDot={{ r: 8 }}
              />
            </LineChart>
          </ResponsiveContainer>
        </section>

        <section className="bg-white p-6 rounded-xl shadow-md">
          <h2 className="text-xl font-semibold mb-4 text-amber-800">
            Cervezas Más Vendidas
          </h2>
          <ResponsiveContainer width="100%" height={300}>
            <BarChart data={cervezasData}>
              <CartesianGrid strokeDasharray="3 3" />
              <XAxis dataKey="name" />
              <YAxis />
              <Tooltip />
              <Bar dataKey="pints" fill="#FBBF24" radius={[6, 6, 0, 0]} />
            </BarChart>
          </ResponsiveContainer>
        </section>
      </div>
    </div>
  );
}
